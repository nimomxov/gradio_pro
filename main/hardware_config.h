/**
 * @file hardware_config.h (main/)
 * @brief REDIRECT — canonical file is components/core/hardware_config.h
 *
 * This file exists only for backward compatibility with any includes
 * that reference "hardware_config.h" from main/. Do NOT add definitions
 * here — all hardware pin/config constants live in components/core/hardware_config.h.
 */
#pragma once
#include "components/core/hardware_config.h"   /* resolves to components/core/hardware_config.h via IDF include path */
